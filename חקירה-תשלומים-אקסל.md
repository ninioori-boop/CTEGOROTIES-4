# חקירה: למה התשלומים לא מופיעים בדוח מהאקסל

## זרימת הנתונים (בלי לשנות קוד)

1. **אקסל → זיכרון**: רק הגיליון הראשון (`SheetNames[0]`), `sheet_to_json(..., { header: 1 })` → מערך של שורות, כל שורה מערך תאים.
2. **שורת כותרת**: הקוד בוחר את **השורה הראשונה** (מתוך 15 השורות הראשונות) שמכילה אחת מהמילים:  
   `שם בית עסק` / `שם בית העסק` / `בית עסק` / `תיאור` / `פרטים` / `שם העסק`.  
   שורה זו נחשבת ל־`headerRow`, וכל העמודות מפוענחות ממנה.
3. **עמודת תשלומים (voucherOrDetail)**: מתעדכנת **רק** אם תא הכותרת מתאים לאחד מהתנאים הבאים (כולם עם `includes` על המחרוזת המלאה):
   - המילה **שובר** **ו**־(**פירוט** או **מספר** או **מס'**)
   - או **פירוט נוסף**
   - או **פר שובר** (כולל רווח: `פר שובר`)
   - או **מספר** **ו**־**פירוט** (ביחד באותה כותרת)

   דוגמאות שיזוהו:  
   `מספר שובר פירוט נוסף`, `מס' שובר פירוט`, `פירוט נוסף`, `פר שובר פירוט נוסף`.

   דוגמאות ש**לא** יזוהו כעמודת תשלומים:  
   כותרת רק **פירוט** (בלי "נוסף"), **מס תשלום** (בלי גרש ב־מס'), **פרטי תשלום**, **מספר עסקה** (בלי פירוט).
4. **עמודת הערות (notes)**: מתעדכנת אם הכותרת מכילה **הערות** / **הערה** / **פירוט** (אבל **לא** "פירוט נוסף").  
   כלומר עמודה עם כותרת **פירוט נוסף** לא נחשבת הערות; עמודה עם כותרת **פירוט** בלבד נחשבת הערות.
5. **איך נבנה הטקסט לכל שורה**:  
   `notesAndVoucher = [notes, voucherCell].filter(Boolean).join(' ')`  
   ואז מתבצע חילוץ תשלום מ־`notesAndVoucher`, מ־`transactionType`, ואם לא נמצא – סריקה על **כל תא בשורה** (`row[c]` עבור `c = 0..row.length-1`).
6. **אורך שורה**:  
   `row` הוא מה ש־SheetJS מחזיר. לרוב תאים ריקים בסוף שורה לא מופיעים במערך, אז `row.length` יכול להיות **קצר** מעמודת הכותרת.  
   אם עמודת התשלומים היא למשל עמודה 10 (אינדקס 9) ו־`row.length === 8`, אז `row[9]` הוא `undefined` – לא קוראים את ערך התשלום **וגם** לא נסרק בתא הזה ב־"סריקת כל תאי השורה".
7. **כניסה לנתיב החכם**:  
   הנתיב שמוסיף תג תשלום לטקסט רץ רק אם נמצאת **עמודת סכום** (chargeAmount או transactionAmount).  
   אם לא מזוהה אף אחת מהן, הקוד עובר ל־`oldFormatDataForAI`, שלא מוסיף תשלומים לטקסט.
8. **מאגר vs AI**:  
   אחרי בניית הטקסט, `splitLinesByDatabase` מפצל: שורות שמתאימות למאגר → `dbExpenses` (עם `installment_info` אם פורסר מהשורה), השאר → `aiText` ל־API.  
   אם **כל** השורות נכנסות למאגר, `hasAiContent` false ולא קוראים ל־`mergeDescriptionsFromSource` – התשלומים יכולים להגיע רק מפירוס ב־`splitLinesByDatabase` (כלומר חייבים להיות כבר בטקסט ש־`formatDataForAI` יוצר).

---

## סיכום סיבות אפשריות לכך שהתשלומים לא מופיעים

| # | סיבה | איך לבדוק באקסל |
|---|------|-------------------|
| 1 | **כותרת עמודת התשלומים** לא מתאימה לאף תנאי למעלה (למשל רק "פירוט", או "מס תשלום" בלי שובר/פירוט נוסף). | לפתוח את האקסל, לבדוק את **שורת הכותרות** ולוודא איך בדיוק נקראת העמודה שמכילה "תשלום X מתוך Y". |
| 2 | **שורת כותרת** – בשורות 0–14 אין שורה שמכילה "שם בית עסק" / "תיאור" / "פרטים" וכו'. | לבדוק אם יש כותרת כזו ואם יש שורות ריקות/כותרות ביניים לפנייה (למשל "עסקאות בארץ") שמדחיפות את שורת הכותרות האמיתית מעבר לשורה 14. |
| 3 | **אורך שורות** – בשורות הנתונים יש פחות תאים מאשר בשורת הכותרת, ועמודת התשלומים נמצאת **מימין** לתאים האחרונים שיש בשורה. | לבדוק אם בשורות עם איקאה/תשלומים יש תאים ריקים באמצע או בסוף; אם העמודה עם "תשלום 6 מתוך 12" נמצאת אחרי תאים ריקים, ייתכן ש־`row.length` קצר והיא לא נסרקת. |
| 4 | **עמודת סכום** לא מזוהה (לא "סכום חיוב" / "סכום עסקה" וכו') → מעבר ל־`oldFormatDataForAI` בלי תשלומים. | לבדוק איך בדיוק נקראת עמודת הסכום החודשי בשורת הכותרות. |
| 5 | **תוכן התא** – הטקסט בתא לא בדיוק "תשלום X מתוך Y" (ריווח, תו מיוחד, או ניסוח אחר). | לבדוק תא אחד שמכיל תשלום: להדביק את הערך המדויק (או צילום מסך) ולוודא שהפורמט תואם למה ש־`extractInstallmentInfoFromNotes` מחפש (למשל `תשלום\s*(\d+)\s*מתוך\s*(\d+)`). |

---

## מה לשלוח כדי לדייק את החקירה

בלי לשנות קוד, כדאי לראות:

1. **צילום מסך או העתקה של שורת הכותרות** מהאקסל (השורה עם "שם בית עסק", "סכום חיוב", עמודת התשלומים וכו').
2. **שורה אחת מלאה** של עסקה בתשלומים (למשל איקאה) – כל התאים בשורה, או צילום מסך של השורה הזו.
3. **מספר השורה** באקסל שבה מופיעה שורת הכותרות (שורה 1 = אינדקס 0 וכו').

עם זה אפשר לזהות בדיוק איזה תנאי בזיהוי הכותרות או באורך השורה גורם לכך שהמערכת לא "תקלוט" את העסקאות בתשלומים.
